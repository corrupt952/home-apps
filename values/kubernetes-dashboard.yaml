protocolHttp: 'true'

extraArgs:
- '--enable-skip-login'

ingress:
  enabled: 'true'
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
  - dashboard.home
  - dashboard.zuki.page

rbac:
  clusterReadOnlyRole: 'true'
